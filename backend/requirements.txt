# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ── Settings / validation ──────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── ML / NLP ───────────────────────────────────────────────────────────────
torch==2.3.0
transformers==4.41.0
safetensors==0.4.3          # required for model.safetensors weights
sentencepiece==0.2.0        # tokenizer dependency for some BERT variants

# ── Explainability ─────────────────────────────────────────────────────────
shap==0.45.1
numpy==1.26.4

# ── Database clients (real — used when mocks are swapped out) ──────────────
supabase==2.4.6             # Supabase Python client
neo4j==5.20.0               # Official Neo4j driver

# ── Batch ingestion / export ────────────────────────────────────────────────
pandas==2.2.2                # CSV parsing (POST /batches/csv) and export building only
python-multipart==0.0.9      # required by FastAPI for UploadFile / multipart form parsing

# ── Testing ────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0               # async test client for FastAPI (TestClient replacement)

# ── Utilities ──────────────────────────────────────────────────────────────
python-dotenv==1.0.1        # load .env in non-Pydantic contexts (workers)
